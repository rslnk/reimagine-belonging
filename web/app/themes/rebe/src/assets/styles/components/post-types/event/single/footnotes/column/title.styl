/**
 * Footnotes title
 */
.c-footnotes__title
  @extend .o-heading

  font-weight $regular
  font-size $micro
  letter-spacing rem(1px)

  +below('m')
    margin-bottom rem(15px) // space between title and the list

  +between('m', 'l')
    margin-bottom rem(20px)

  +between('l', 'xl')
    margin-bottom rem(20px)

  +above('xl')
    margin-bottom rem(25px)
    font-size $milli
