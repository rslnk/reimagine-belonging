/**
 * Workshop post main content
 *
 * Stacks on top of sidebar on small screens
 *    - Displayed next to sidebar on larger screens
 */
.c-workshop__main

  +below('l')
    stack()
    margin-bottom rem(30px) // space between main and sidebar

  +above('l')
    column(2/3)
    margin-bottom 0
