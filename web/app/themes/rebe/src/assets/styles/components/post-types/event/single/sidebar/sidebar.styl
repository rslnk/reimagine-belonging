/**
 * Event post sidebar
 *
 *    - Wrapped inside `c-event__content`
 *    - Stacks under main content on mobile screen
 *    - Displayed next to main content on desktop screen
*/
.c-event__sidebar
  stack()
  margin-bottom rem(10px)
  +media-query(mediatype, bp-tablet)
    margin-bottom rem(20px)
  +media-query(mediatype, bp-desktop)
    unstack()
    column(1/3)
    margin-bottom rem(30px)
